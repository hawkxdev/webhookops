[project]
name = "management"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "celery>=5.6,<5.7",
    "django>=5.2,<5.3",
    "django-environ>=0.14.0",
    "psycopg[binary]>=3.3.4",
    "shared",
]

[tool.uv.sources]
shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "celery-types>=0.26,<0.27",
    "django-types>=0.24.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/management"]
