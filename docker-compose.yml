services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:4-management
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD}
    ports:
      - "127.0.0.1:5672:5672"    # AMQP, протокол брокера
      - "127.0.0.1:15672:15672"  # веб-панель
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 15s
      timeout: 10s
      retries: 5
    restart: unless-stopped
    
  redis:
    image: redis:7
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

# === Прикладные сервисы ===
# ingest:
#   build: ./ingest
#   command: uvicorn ingest.main:app --host 0.0.0.0 --port 8000
#   env_file: [.env]
#   depends_on:
#     postgres:
#       condition: service_healthy
#   ports:
#     - "127.0.0.1:8000:8000"
#   restart: unless-stopped
#
# management:
#   build: ./management
#   command: python manage.py runserver 0.0.0.0:8001
#   env_file: [.env]
#   depends_on:
#     postgres:
#       condition: service_healthy
#   ports:
#     - "127.0.0.1:8001:8001"
#   restart: unless-stopped
#
# worker:
#   build: ./management
#   command: celery -A management worker -l info
#   env_file: [.env]
#   depends_on:
#     postgres:
#       condition: service_healthy
#     rabbitmq:
#       condition: service_healthy
#   restart: unless-stopped

volumes:
  postgres_data:
